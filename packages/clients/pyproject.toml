[project]
name = "civic-clients"
version = "0.0.0"
description = "Thin async clients for Postgres, Neo4j, OpenSearch, and MinIO/S3."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/civic_clients"]
