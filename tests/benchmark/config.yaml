# Threshold gates (Phase 6) — tightened after semantic gold set expansion.
# The offline VerifyPipeline (no live graph/lexical) correctly abstains on OData
# JSON input, so claim/verdict F1 remains 0.0 until live retrieval is wired.
# min_rows enforces gold-set coverage breadth.
version: 1
min_rows: 4
f1_verdict: 0.0
f1_claim_typing: 0.0
recall_evidence: 0.0
