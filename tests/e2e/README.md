# tests/e2e/

End-to-end tests live here in v2+ when multi-service browser/HTTP tests are needed.

For v1, cross-cutting integration tests are in `tests/integration/`, smoke tests in
`tests/smoke/`, and per-package unit tests live alongside each workspace member
(e.g. `services/claim_decomposition/tests/`, `apps/api/tests/`).
